# drupal-article-export-to-json
Drupal Article Export to JSON is a lightweight Drupal module that enables the export of Article content into structured JSON format. It provides a simple and reliable way to expose article data for use in external applications, headless front‑ends, APIs, integrations, or data migrations.
